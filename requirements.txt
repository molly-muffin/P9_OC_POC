# Combined dependencies — Streamlit Cloud reads this file at the repo root.
# Dashboard
streamlit>=1.35.0
plotly>=5.20.0
timm>=1.0.3

# Shared (dashboard + notebook)
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0
Pillow>=10.0.0
matplotlib>=3.8.0

# Notebook only (harmless for the dashboard deployment)
seaborn>=0.13.0
scikit-learn>=1.4.0
